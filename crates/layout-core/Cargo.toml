[package]
name = "layout-core"
version = "0.1.0"
edition = "2021"
description = "Deterministic placement and routing contracts for Copperline AutoLayout"
license = "MIT"

[lib]
crate-type = ["rlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
